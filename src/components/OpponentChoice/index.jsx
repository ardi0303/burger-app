import React from "react";

const OpponentChoice = ({ opponentChoice }) => {
  return (
    <div className="text-center text-white text-lg">
      Opponent's Choice: {opponentChoice}
    </div>
  );
};

export default OpponentChoice;
